VERSION = 1,
option BF.PregnancyDuration {
    type            = integer,
    min             = 2,
    max             = 270,
    default         = 14,
    page            = BF,
    translation     = BF_Pregnancy_Duration,
}

option BF.PregnancyRecovery {
    type            = integer,
    min             = 0,
    max             = 56,
    default         = 7,
    page            = BF,
    translation     = BF_Pregnancy_Recovery,
}

option BF.WombMaxCapacity {
    type            = double,
    min             = 0.1,
    max             = 3,
    default         = 1.0,
    page            = BF,
    translation     = BF_Womb_Max_Capacity,
}

option BF.MilkCapacity {
    type            = double,
    min             = 0.3,
    max             = 5,
    default         = 1.0,
    page            = BF,
    translation     = BF_Milk_Capacity,
}

option BF.MilkExpiration {
    type            = integer,
    min             = 1,
    max             = 365,
    default         = 7,
    page            = BF,
    translation     = BF_Milk_Expiration,
}
